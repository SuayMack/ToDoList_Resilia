class TarefasModel {
  constructor(titulo, dt_criacao, descricao, status) {
    this.titulo = titulo
    this.dt_criacao = dt_criacao
    this.descricao = descricao
    this.status = status
  }
}

export default TarefasModel